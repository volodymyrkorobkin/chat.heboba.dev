services:
    chat:
        build:
            context: .
            dockerfile: Dockerfile
            args:
                BUILD_DIR: dist
        ports:
            - "80:80"
        restart: unless-stopped
